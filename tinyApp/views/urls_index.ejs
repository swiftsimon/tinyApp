<!DOCTYPE html>

<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>

    <header>
        <% include partials/_header %>
    </header>
    <ul><% for (urlFound in urls) { %>
        <li>
          <%= urlFound + ":" + urls[urlFound].longURL %>
        </li>
        <!--insert <%= urlFound %> to pull the id number when delete button is pressed -->
      <form method="POST" action="/urls/<%= urlFound%>/delete">
        <input type="submit" value="delete">
      </form>
      <form method="POST" action="/urls/<%= urlFound %>">
        <input type="submit" value="edit">
      </form>
      <% } %>

    </ul>

    <p><a href='/urls/new'> shorten a new url </a></p>


</body>
</html>